import {FiltroPeriodoPipe} from './filtro-periodo.pipe';

describe('FiltroPeriodoPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroPeriodoPipe();
    expect(pipe).toBeTruthy();
  });
});
